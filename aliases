# Unix
alias mkdir='mkdir -p'
alias ...='../..'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias v="$EDITOR"

# Bundler
alias b="bundle"

# Rubygems
alias gi="gem install"
alias giv="gem install -v"
